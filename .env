


# =====================
# Konfiguracja bazy danych (Docker)
# =====================
DB_HOST=db
DB_PORT=3306
DB_NAME=moja_strona
DB_USER=root
DB_PASS=example

# =====================
# Alternatywa lokalna (XAMPP) – odkomentuj w razie potrzeby
# =====================
# DB_HOST=127.0.0.1
# DB_PORT=3307
# DB_NAME=moja_strona
# DB_USER=root
# DB_PASS=example

# =====================
# Przydatne komendy (informacyjnie)
# =====================
# --- Docker ---
# Uruchomienie kontenerów:   docker compose up -d --build
# Restart aplikacji:          docker compose restart app
# Logi bazy:                  docker compose logs -f db
# Import SQL (PowerShell):    Get-Content -Raw .\databases\schema.sql | docker exec -i mysql-db mysql -uroot -pexample moja_strona

# --- GitHub ---
# Inicjalizacja repo:         git init && git add . && git commit -m "Init"
# Ustawienie głównej gałęzi:  git branch -M main
# Dodanie zdalnego:           git remote add origin https://github.com/TWOJ-USER/TWOJE-REPO.git
# Wypchnięcie zmian:          git push -u origin main

#
#docker exec -it mysql-db mysql -u root -ppassword moja_strona
# Wpisz hasło: example
# W konsoli MySQL:
# mysql> USE moja_strona;
# mysql> SOURCE /docker-entrypoint-initdb.d/01-schema.sql;
